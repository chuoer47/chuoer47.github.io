# Chuoer47 Blog

基于 VitePress 的个人技术博客，文章统一存放在 `docs/posts`。

## 开发

```sh
npm ci
npm run docs:dev
```

## 构建

```sh
npm run docs:build
```

构建产物位于 `docs/.vitepress/dist`，由 GitHub Actions 发布到 GitHub Pages。
